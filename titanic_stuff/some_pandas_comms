n = pd.read('file_name', header=False)

df.fillna(n)
df.iloc[n:n,n]
df.values[n]
df.index[n]
df.columns
df.head()
df.tail()
df.info()
df.describe()

df.iloc[:,2].value_counts().sort_index()

df.value_counts()
df.sort_index()
df[1].value_counts().plot(kind='(bar/pie/scatter)'
df.rename(columns={'1':'2'})
df.drop('columnName',axis=1,inplace=True)
pd.to_numeric(df)
pd.get_dummies(df)

pd.concat([df1, df2, df3], axis=(0 or 1, (vertical or horizontal)))
>vertical concat will try to put them in order in a column
>horizontal concat will put them side by side <- usually most helpful

np.any()
np.all()

np.any(np.isnan(my_data))


change test data size to 0.2 of data
use data other than class/embarked
